# 13C QA：敌人角色差异与同屏协同

日期：2026-07-20

## 交付范围

- 新增 `scripts/enemy_combat_director.gd`，每个房间独立管理近战、远程、机动行动通道；令牌具备持有者校验、超时回收、同敌人互斥和远程射击间隔，避免三名敌人无节奏地同时压制。
- 六种角色接入稳定编队槽：机械鹰 `harrier`、虚空飞龙 `ambusher`、风暴法师 `controller`、灵魂盾卫 `vanguard`、灵魂枪骑 `lancer`、灵魂炮手 `artillery`。未配置角色的旧敌人继续使用原行为，不破坏旧关卡。
- 机械鹰采用青色俯冲预警、突击、脱离三段状态；飞龙采用环形预警、三重残影瞬移、十字弹幕；法师按受击阶段升级为 1/3/5 发弹幕。
- 盾卫获得正面减伤、受击火花和盾击；枪骑士获得长距离品红直线预警与突刺；炮手维持安全距离，在单发和三连射之间轮换。

## TDD 与回归证据

- `tests/test_stage_13_c.gd` 先以缺失导演、角色映射、攻击状态与截图钩子的 RED 契约启动，再分批转为 GREEN；包含运行时令牌互斥、超时、远程间隔和编队槽断言。
- 最终目视 QA 又先后增加并验证两条失败用例：暂停物理后必须强制显示玩家动画；虚空截图玩家必须落在桥面安全坐标。两条均先 RED、修复后 GREEN。
- 历史 `test_stage_10_6_5.gd` 原先对整份 `main.gd` 搜索暂停物理字符串，会误判截图专用代码；断言已收窄到 `_play_shaft_transition()`，仍严格保证可操作下井不暂停玩家物理。
- 最终全量：`TOTAL=16`、`FAILED=0`；每项 `EXIT=0`，全部 `ERROR:` / `SCRIPT ERROR` 扫描为 0。
- 编辑器全项目解析：`EDITOR_EXIT=0`、`EDITOR_ERRORS=0`。

## godot-capture 目视验收

以下 PNG 均由 `Godot Engine v4.7.rc.custom_build.df6235838` 实际运行生成，并已逐张打开查看：

- `screenshots/13C/void-squad/frame_0.png` 至 `frame_7.png`：确认三名虚空敌人错位出生，机械鹰青色俯冲线、飞龙传送环与残影、法师蓄力环和弹体可读；玩家落在桥面且不再被巨型虚空核心遮挡。
- `screenshots/13C/castle-squad/frame_0.png` 至 `frame_7.png`：确认盾击、炮手蓄力、枪骑直线预警和突刺按导演节奏交替出现；房间标题已移除，传送提示不遮挡战斗主体。
- 目视过程中实际修复了四类缺陷：房间大标题遮挡、玩家/敌人出生重叠、暂停无敌帧导致玩家隐身、虚空玩家悬空及背景吞轮廓。
- `screenshots/13C/release-void/frame_0.png` 至 `frame_7.png`：由发布目录 EXE 直接启动生成；关键帧再次确认俯冲、瞬移、施法状态已经进入 PCK。

## 运行与错误扫描

- 源码 Godot 图形主程序运行 240 帧：`RUNTIME_EXIT=0`、`RUNTIME_ERRORS=0`。
- 两组源码战斗连拍：进程均 `Exit 0`，每组 8 帧，错误数均为 0。
- 发布版虚空敌群连拍：`RELEASE_EXIT=0`、`RELEASE_ERRORS=0`、`RELEASE_FRAMES=8`。

## 素材与许可证

- 本阶段未下载或生成新的外部素材。敌人预警线、蓄力环、瞬移残影、盾牌火花与弹幕均使用 Godot 程序化绘制及项目既有已登记素材，因此没有新增第三方署名或许可证义务。

## 发布证明

- 使用项目指定的源码编译 Godot 和 Windows release 模板导出到 `E:\Godot\release\暗影机械城`，`EXPORT_EXIT=0`。
- `ShadowMechCity.exe`：68,143,616 bytes；2026-07-20 22:30:04；SHA-256 `1529C04A88DAAAD7C2683259388EBBE1B730F10F9DC9FE9A1EA81C0CE7619B0F`。
- `ShadowMechCity.pck`：10,895,088 bytes；2026-07-20 22:30:05；SHA-256 `754715DD71B3FF359F31AF5009191D8D0D212B17AF9B5B859A8EBFC2A7CC4E9B`。
- EXE 是稳定的源码编译运行模板，所以哈希可与上一版相同；本轮玩法变更位于同目录更新后的 PCK，发布版直启连拍证明其已装载 13C 内容。

## 诚实边界

- 自动化与连续截图证明角色状态、调度互斥、预警可读性、包体内容和无脚本错误；它不能替代策划长时间手柄/键鼠试玩后的数值、伤害与密度平衡。
- 13D 的命中停顿、硬直/击退、材质音效、镜头震动与技能特效统一仍未完成，继续保留为下一批次。
