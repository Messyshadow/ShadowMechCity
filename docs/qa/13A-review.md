# 13A QA：成长界面、装备仓库与统一属性

日期：2026-07-20

## 交付范围

- 原列表式技能页替换为四页圆形空间图谱：基础、身法、战斗、探索；战斗页包含刀剑、铁锤、蒸汽枪炮、双刀、长枪、弓弩家族切换。
- 右侧技能教学以独立 `SubViewport` 播放角色/武器预览，并显示触发按键、使用方法、效果、当前到下一级成长和前置技能；组合技与被动技能均有明确教学。
- 双刀、长枪、弓弩在 13A 仅显示不可加点的锁定家族入口，真实装备、获取流程与招式仍归 13B。
- 背包替换为武器/防具/饰品/消耗品/材料/任务六分类三栏界面；左侧为真实主角与当前武器投影及六装备槽，中部为稳定源索引网格，右侧为来源、词条、带符号属性差和装备/卸下/强化操作。
- 新增统一属性解析器、装备词条映射和玩家属性快照；生命、移速、技力上限/回复、主动技能伤害与冷却均从同一快照读取。
- 存档版本升级为 13，旧技能 ID、背包、装备、能力和武器解锁原样迁移；迁移连续执行两次结果一致。

## TDD 与回归证据

- `tests/test_stage_13_a.gd` 依次验证并实际出现预期 RED：缺少属性解析器、空间图谱、迁移器、圆形技能 UI、分类背包/对比器、玩家快照接入和确定性截图钩子。
- 每一批最小实现后转为 GREEN。图谱校验曾把 `ultimate -> spin/crit -> atk` 的合法菱形汇聚误判为循环；输出具体错误后将校验改为递归路径检测，保留数据并消除误判。
- 动态主场景实例化另外捕获两处编辑器静态扫描未报出的 GDScript 类型推断错误，均在提交前修复并复验。
- 视觉修复前实拍发现基础页最右节点和刀剑终结节点被画布裁切、右侧详情需要滚动才能看到完整效果；节点坐标增加 720p 安全钳制，预览高度收紧，并补充当前等级到下一等级文本后重新实拍。
- 当前完整契约基线为 14 项：下行入口、10.5、10.6、10.6.3、10.6.4、10.6.5、10.7、10.8、10.9、12.1、12.2、12.3、12.4、13A；阶段中途全量运行 `FAILED=` 为空。

## godot-capture 目视验收

所有画面均由源码编译 `Godot Engine v4.7.rc.custom_build.df6235838` 实际启动 Vulkan 渲染生成；7 次最终截图运行退出码均为 0，输出未出现有效 `ERROR` / `SCRIPT ERROR`。

- `screenshots/13A/skill-basic-passive.png`：基础页六节点全部位于安全区；生命强化选中双环清晰，自动生效、用途、效果、1级→2级成长和前置完整显示。
- `screenshots/13A/skill-mobility.png`：身法图谱分叉和绿色连线清楚；迅捷冲刺的 `Shift / L`、被动触发方式与冷却成长同屏可读。
- `screenshots/13A/skill-sword-combo.png`：刀剑六节点和终结节点均未裁切；旋风斩显示 `J → J → J`、组合用法与已达最高等级，角色预览与详情无重叠。
- `screenshots/13A/skill-exploration-locked.png`：探索能力根统一使用紫色锁定环；壁行共鸣明确显示贴墙按键、未获取与分支锁定状态。
- `screenshots/13A/inventory-armor-compare.png`：传奇胸甲候选与已装备胸甲对比显示防御 `+4`、生命 `+3`；左侧当前铁剑和六槽状态、中部源物品、右侧操作一致。
- `screenshots/13A/inventory-accessory.png`：天龙指环显示攻击、暴击率、吸血的带符号增益，装备目标为戒指，排序后仍对应原库存源索引。
- `screenshots/13A/inventory-empty-category.png`：材料分类为空时保持完整三栏结构，并显示具备世界观语气的获取提示，没有布局塌陷。

## 错误扫描与资产说明

- 图形截图、13A 契约、编辑器解析和动态主场景实例化均扫描全部 `ERROR` / `SCRIPT ERROR`。动态退出只出现 Godot 已知的 `ObjectDB instances` / `resources still in use` 缓存噪声，按项目 Playbook 过滤，不计为运行错误。
- 本批次没有下载或生成新第三方素材；角色、武器与场景预览复用仓库已有资源，既有来源与许可证记录不变。新增的节点环、连线、面板、机械圆环和背景线条均为 Godot 程序化绘制，无新增外部许可义务。

## 诚实边界

- 自动截图能证明 1280×720 布局、选择状态、数据对应和预览画面，不能替代真实玩家长时间用鼠标/WASD/方向键操作后的导航手感评价。
- 属性快照与旧值由自动契约覆盖，但技能点经济、装备掉落节奏和各构筑长期平衡仍需策划使用发布 EXE 试玩。
- 双刀、长枪、弓弩真实装备与组合技、敌人角色/协同、多层打击感分别保留在 13B、13C、13D，本报告不宣称它们已完成。
