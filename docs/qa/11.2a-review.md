# 11.2a 装备数据迁移与报价验收报告

日期：2026-08-30

结论：通过（仅完成 11.2a；11.2b 升级终端、11.2c 个性补丁、11.2d 品牌套装仍未开始）

## 交付范围

- 存档从 v16 升级为 v17。每件持久装备获得稳定 `item_instance_id`、`brand_id`、`quality`、`patch_level`、`patches` 和 `cosmetic_id`。
- 迁移遍历背包和六个装备槽，使用保存位置与旧装备内容生成稳定 ID，并检测重复；非装备材料、消耗品和任务物品保持原结构。
- 旧 `lv` 不清零，完整复制为首次 `patch_level`；后续属性和报价以 `patch_level` 为权威，`lv` 暂作旧 UI 兼容镜像。
- 首批五品牌为铸魂工坊、虚空蒸汽局、深渊孵化所、遗迹符文院、量子虚空研究所，默认品牌按装备槽位确定，混搭不会受到通关限制。
- 新增纯函数 `upgrade_quote()`：费用由品质、当前补丁阶段和补丁稀有度共同决定；新增无副作用 `attribute_preview()`，返回升级前后六项装备词条。
- 继续复用 `Game.coins`，没有建立齿轮币、补丁币或其他平行钱包。
- 现有背包详情加入只读“补丁报价预览”：显示品牌、品质、阶段、金币报价及预计属性增益。11.2a 不包含卫星投射、二次确认或新终端。

## 契约与回归

1. 生产代码实现前运行 `test_stage_11_2a.gd`，首次 RED 12 项；增加只读面板要求后再次 RED 2 项。
2. 实现后 11.2a 契约转绿；迁移重复执行结果一致，金币、技能、旧等级和非装备物品均保留。
3. 全部 `tests/test_*.gd`：`TOTAL=30 FAILED=0`。
4. 源码 Godot 编辑器解析通过；主场景连续 240 帧：`ACTIONABLE_ERROR_COUNT=0`。
5. godot-capture 日志扫描全部 `ERROR:` / `SCRIPT ERROR`：0 项。退出时既有 ObjectDB/ResourceCache 统计噪声不含 GDScript 回溯。

## godot-capture 目视验收

源码引擎：`E:\SourceCode\Games\engine\big_engine\godot\bin\godot.windows.editor.x86_64.console.exe`

- `screenshots/11.2a/patch-preview.png`：传奇“虚空壁垒”迁移为“遗迹符文院 / 传奇品质 / 补丁 Lv.2→Lv.3”；核心同步报价 1087 金币，防御 +0.60、生命 +0.36。
- 已真实查看 PNG：品牌、品质、报价、属性增益与装备比较同屏可读；右栏无截断，三栏和底部操作区无重叠。
- SHA-256：`A4070EC1625CFB2C86102FCA27541DD7C3E712E7CB5B18AA694D66F126E7533E`。

## 素材与许可

本批未新增或下载外部美术、字体、音频与生成式素材；界面沿用项目现有程序化 UI，因此没有新增许可证条目。

## 后续边界

下一步 11.2b 才实现枢纽量子升级终端、分类选择、卫星投射演出、确认/取消、余额拒绝与落盘。当前不得将 11.2 或阶段 11 整体标记完成。

## 发布验证

- 目录：`E:\Godot\release\暗影机械城`；预设：`Windows Desktop`。
- 使用自编译 release 模板导出；发布版连续运行 240 帧，退出码 0，`ACTIONABLE_ERROR_COUNT=0`。
- `ShadowMechCity.exe`：68,143,616 字节，2026-08-30 00:54:24，SHA-256 `1529C04A88DAAAD7C2683259388EBBE1B730F10F9DC9FE9A1EA81C0CE7619B0F`。
- `ShadowMechCity.pck`：11,057,172 字节，2026-08-30 00:54:24，SHA-256 `812A1185595D50420DC4200B21B4AA3E31A9DF90AA4DD34A75A41C86CE0B15AB`。
