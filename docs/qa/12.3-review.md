# 12.3 QA：支线任务与收藏档案

日期：2026-07-16

## 交付范围

- `N` 任务日志增加“主线 / 支线 / 收藏”三页签，沿用暗钢框架与键鼠导航。
- 五条支线分别对应炉心铸造师、雾酿术士、轨图解析师、遗物收藏家、赏金追踪者；NPC 首次交谈后解锁。
- 目标从武器数、记忆核心数、隐藏房数和 Boss 击败数回算，兼容 12.3 以前的存档，不要求重刷。
- 七枚 10.8 记忆核心升级为七区域具名档案；未发现记录只显示区域与加密轮廓，已发现后显示完整正文。
- HUD 追踪器支持支线数量目标；标题页可见构建标记更新为“阶段 12.3 · 支线与记忆档案”。

## TDD 证据

`tests/test_stage_12_3.gd` 在生产代码实现前运行，因 `SIDE_ORDER` 不存在而以 `SCRIPT ERROR` 失败；实现数据与求值器后，首次 GREEN 检查发现“七区猎杀令”的测试样本只提供了五个 Boss 事实，修正夹具为七个真实 Boss ID 后通过：`PASS stage 12.3 side quest and collectible archive contracts`。

## godot-capture 目视验收

- `screenshots/12.3/side-quests.png`：五条支线、2/5 完成度、委托人、摘要与 6/6 数量目标在 1280×720 安全区内完整，无文字裁切或 HUD 穿透。
- `screenshots/12.3/collectibles.png`：4/7 已同步状态清楚；三份未发现档案隐藏标题和正文，已发现档案显示来源区域与世界观文本；七条列表均可见。
- 两次图形运行均由源码编译 Godot `4.7.rc.custom_build.df6235838` 生成，输出未出现 `ERROR` / `SCRIPT ERROR`。

## 回归与发布

- 全部 11 个契约通过：`test_downward_portals.gd`、阶段 10.5、10.6、10.6.3、10.6.4、10.6.5、10.7、10.8、12.1、12.2、12.3，`TOTAL=11 FAILED=0`。
- 编辑器全项目解析退出码 0；主场景无头运行退出码 0。只出现技能文档允许过滤的退出期 `1 resources still in use` / `ObjectDB instances`，无运行期错误。
- 使用源码编译 `Godot Engine v4.7.rc.custom_build.df6235838` 与项目自定义 `template_release` 导出。
- `E:\Godot\release\暗影机械城\ShadowMechCity.exe`：68,143,616 bytes，2026-07-16 23:06:44，SHA-256 `1529C04A88DAAAD7C2683259388EBBE1B730F10F9DC9FE9A1EA81C0CE7619B0F`。
- `ShadowMechCity.pck`：10,785,344 bytes，2026-07-16 23:06:44，SHA-256 `86C26BCC0E2DC7917438D418B4293423DDAF852F14962C4D0C732F018781EF6D`。
- 从发布目录直接启动 EXE 并生成 `screenshots/12.3/release-collectibles.png`，退出码 0、无 `ERROR` / `SCRIPT ERROR`；实际画面与编辑器收藏页一致。
- 自定义 release 模板不支持编辑器参数 `--path`；首次带该参数的诊断启动明确拒绝，随后改为从 EXE 所在目录直接运行并通过。这不影响资源管理器双击使用。

## 延后设计记录

用户确认的技能树重做方向已经写入 `docs/future_systems.md`：基础、移动/身法、战斗、探索页面；圆形树节点、四向导航、外圈高亮、右侧动画与详情；战斗页按刀剑、铁锤、蒸汽炮/手枪、双刀、长枪、弓弩拆分。本阶段不改动现有技能树。
