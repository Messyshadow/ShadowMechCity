# 阶段 13.3.1 验收报告：双向可操作机械竖井

日期：2026-07-29

引擎：源码编译 Godot `4.7.rc.custom_build.df6235838`

结论：通过（阶段 13.3 的专项完善；13.3 场景统一父项仍继续开发）

## 交付内容

- 原 620px 下行过场升级为方向化竖井组件；保留真实 Player 物理、中央自由落线、连续双墙和三处交错单向踏板。
- 普通上行入口新增 480px 可操作路线：从井底出发，可使用基础墙滑/墙跳，也兼容已获得的攀墙能力。
- 隐藏 `up` 传送阵继续走原隐藏入口逻辑，没有被误路由为竖井；横向房门同样不受影响。
- 上行入口提示由泛化的“启动传送阵”改为明确的 `[E / Enter] 向上攀登`。
- 相机按方向切换纵向前瞻并临时扩展井底边界，结束后恢复房间限制。
- 上行 12 秒未完成时取消过场并回到当前入口，不错误切换房间；下行保留防卡死完成保险。
- `SHOT_SHAFT_DIRECTION=down|up` 支持双向 8 帧确定性连拍。

## godot-capture 实际 PNG

验收目录：

- `screenshots/13_3_1/down/frame_0.png` 至 `frame_7.png`
- `screenshots/13_3_1/up/frame_0.png` 至 `frame_7.png`

共 16 张正式验收 PNG，均由源码 Godot 实际运行生成并逐张查看。

目视修复闭环：

1. 首轮下行画面确认入口厚边机械框、梯架、双墙、踏板和中心落线可读，玩家真实向下移动。
2. 首轮上行画面发现玩家位于屏幕外；坐标日志确认是相机下边界仍锁在原房间。修正后井底出生点进入镜头。
3. 第二轮发现 99 秒无敌闪烁会让玩家在截图时隐身；改为 0.6 秒短保护。
4. 自动按键无法稳定产生 `just_pressed` 墙跳；QA 连拍改为调用玩家现有 `_jump/_wall_jump` 动作，正常游戏输入逻辑不变。
5. 最终帧可见玩家从井底起跳、在墙面蹬跳并产生尘屑，镜头随纵向位置平滑移动；墙面连续、踏板未封死中央通路。

## 自动化与错误扫描

- `tests/test_stage_13_3_1.gd`：通过。
- 旧版 `test_downward_portals.gd`、`test_stage_10_6_5.gd`、`test_stage_10_9.gd`：通过。
- 全部 `tests/test_*.gd`：23 / 23 通过。
- 源码编辑器全项目解析：退出码 0。
- 主场景源码 Godot `--headless --quit-after 240`：退出码 0。
- 全部输出扫描：0 条可执行 `ERROR` / `SCRIPT ERROR`。

## 发布证据

使用 `export_presets.cfg` 的 `Windows Desktop` release 预设与源码编译模板：

`E:\SourceCode\Games\engine\big_engine\godot\bin\godot.windows.template_release.x86_64.exe`

发布目录：`E:\Godot\release\暗影机械城`

- `ShadowMechCity.exe`：68,143,616 字节，2026-07-29 20:29:24，SHA-256 `1529C04A88DAAAD7C2683259388EBBE1B730F10F9DC9FE9A1EA81C0CE7619B0F`
- `ShadowMechCity.pck`：10,960,336 字节，2026-07-29 20:29:27，SHA-256 `860E05B163F8690CE689552C43BB94EC8EB8F6FA5D3FEC4D72BF508C81A54ADE`
- 发布版隐藏运行 240 帧：退出码 0，0 条可执行 `ERROR` / `SCRIPT ERROR`。

## 素材与许可证

本轮未新增外部素材。竖井墙板、支架、灯、雾、出口光环和踏板均为 Godot 程序化绘制，沿用项目现有角色与场景资源，因此没有新增第三方许可证。

## 结论

双向竖井已达到可验收状态：下行不再是瞬时切房，上行具备真实墙跳/攀墙路线和明确提示；代码合同、实际移动截图与错误扫描一致通过。
