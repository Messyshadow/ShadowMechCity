# 12.2 QA：主线任务、任务日志与标题主视觉

日期：2026-07-16

## 交付范围

- 四章主线任务链：残响坐标、断裂的传送网、越过虚空天幕、最后的光核。
- `quest_data.gd` 只保存剧情/目标定义，`quest_runtime.gd` 作为不依赖 Autoload 的纯状态求值器。
- 旧存档根据 `visited`、`items`、`dialogue_flags`、`story_flags` 即时回算，不要求重新探索或重打 Boss。
- `quest_flags` 与 `tracked_quest_id` 支持保存、读取、重置、缺字段迁移与无效追踪回退。
- `N` 键任务日志：四章节列表、状态/完成度、剧情摘要、委托人、目标与地点提示、鼠标/键盘选择和追踪切换。
- HUD 任务追踪器：区域标题下显示当前任务与最多两条未完成目标，不遮挡小地图和底部按键栏。
- 开始菜单替换为原创暗影机械城主视觉，使用等比覆盖而非拉伸，并加入暗钢菜单板与可见构建标记“阶段 12.2 · 主线任务系统”。

## TDD 证据

`tests/test_stage_12_2.gd` 首次运行退出码 1，明确报告 14 类缺口：任务持久化字段、任务按键、四章数据、纯运行时、日志/追踪器、主场景截图钩子、标题图片与构建标记均不存在。

实施过程按 RED → GREEN 分段验证：

1. 新增任务数据/求值器后，所有四章低/中/完成进度断言通过，契约只剩存档、UI 与标题缺口。
2. 初版 UI 独立加载暴露 `Identifier not found: Game`；测试保持失败。根因是 `--script` 环境不注册 Autoload 全局标识符，改为运行时安全查找 `/root/Game` 后，两份 UI 脚本真实加载/实例化通过。
3. 标题 PNG 首次导入使用 3 秒编辑器退出窗口，日志明确显示 `Scan thread aborted`；延长到完整扫描后，资源在 303 ms 内成功导入。
4. 最终结果：`PASS stage 12.2 main quest, journal and title art contracts`。

## 全回归与错误扫描

源码编译 Godot：`Godot Engine v4.7.rc.custom_build.df6235838`。

全部 10 个契约通过：

- `test_downward_portals.gd`
- `test_stage_10_5.gd`
- `test_stage_10_6.gd`
- `test_stage_10_6_3.gd`
- `test_stage_10_6_4.gd`
- `test_stage_10_6_5.gd`
- `test_stage_10_7.gd`
- `test_stage_10_8.gd`
- `test_stage_12_1.gd`
- `test_stage_12_2.gd`

回归中发现 `test_stage_10_6.gd` 只接受无空格源码串 `camera.target=player`，而实际代码为等价的 `camera.target = player`，相机目标和 `end_shaft()` 参数恢复均存在。已将断言改为去空格后匹配；未改动竖井运行行为。

编辑器全项目解析与 `main.tscn` 无头运行扫描：0 个非退出噪声 `ERROR` / `SCRIPT ERROR`。唯一输出为 Godot 已知退出期 `1 resources still in use at exit`，按 godot-capture 规则过滤。

## godot-capture 实际 PNG 目视验收

- `screenshots/12.2/title-menu.png`：生成图在 Godot 中正确显示；右侧虚空裂隙、机械塔楼、悬空列车、炉火与钢铁结构清楚，左侧暗区完整承载中文标题、四个按钮及 12.2 标记，无拉伸、裁切或文字冲突。
- `screenshots/12.2/quest-log.png`：1080×650 日志框在 1280×720 视口四边完整；四章状态、摘要、三项目标、地点提示和底部操作说明均清晰可读。
- `screenshots/12.2/quest-tracker.png`：追踪器位于区域标题下方，显示“断裂的传送网”和两条目标；未覆盖右侧小地图、角色、NPC 或底部操作栏。

## 标题资产来源与许可证

- 项目文件：`assets/bg/title/shadow_mech_city_title.png`（1672×941 PNG）。
- 生成方式：OpenAI 内置图像生成工具，随后使用同一工具只调整曝光/中间调；没有下载第三方素材。
- 最终提示核心：原创 16:9 暗黑机械/赛博蒸汽朋克城市，铆接钢塔、齿轮、蒸汽管网、悬空轨道、青蓝/紫色虚空裂隙、少量橙色炉火；左侧保留菜单安全区；无文字、Logo、UI、水印、人物或现成 IP 元素。
- 记录文件：`assets/bg/title/README.md`。

## 视觉与交互结论

- 标题画面已经能直接体现“暗影、机械城、虚空侵蚀”三项主题，并且可见版本标记能证明发布包内容已经更新。
- 日志打开时增加 `Game.menu_open` 并锁定玩家战斗/移动；关闭时计数归零并恢复输入，环境无需暂停。
- 任务组件使用纯数据事实回算，旧存档即使缺少 12.2 字段也能自动进入正确章节。

## 人工试玩边界

自动契约和截图覆盖数据、迁移、UI 安全区、资源导入与按键契约；仍建议策划在发布 EXE 中连续试玩 `N` 开关、鼠标切换追踪、Boss 击败后的章节推进提示，以及长文本阅读节奏。

## 发布包本体验证

待 Git 提交和 Windows 导出后补入 EXE/PCK 时间、大小、哈希、发布 EXE 错误扫描与发布版标题截图。
