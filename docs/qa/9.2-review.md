# 9.2 验收报告 — 主动技能框架（技力/怒气 · 搓招 · 铁剑3技能 · 大招）

> 验收线程（godot-capture）出具 · 提交基线 `5862fd4`（含开发线程自查加固）· 日期 2026-06-27
> 方法：代码审查 + **技能特效连拍**（godot-capture 本次扩展了 `SHOT_SKILL` 技能触发剧本）+ HUD 静态确认。
> 对照开发线程 `docs/qa/9.2-selfcheck.md` 逐项复验。

## ✅ 通过项

1. **K 键过载护栏正确**（代码审查，与自查一致）：`_try_skill` 优先级 = ↑上挑 > (双击同向且仍按住)突进 > 地面波；单按方向跑+K 必为地面波（`_dash_ready=0`）；突进触发即 `_dash_ready=0` 消费防连放。
2. **搓招缓冲正确**：双击窗口 0.28s（`_tap_t`），`_dash_ready=0.32`；释放时再校验 `signf(hx)==_dash_dir`（反向不误放）；双击只 arm 窗口、不改移动（冲刺仍 Shift/L），无移动冲突。
3. **资源系统 HUD 达标**（截图 `screenshots/qa4_skill_ground.png`）：红心下方**技力=蓝条 / 怒气=橙条**，分明，符合 9.1 拍板的护栏；底部按键提示含"K技能 上/→变招、V大招"，缓解搓招发现性。
4. **4 个技能特效全部连拍验证**（`screenshots/qa4_skill_*.png`）：
   - **地面波**(K)：剑气波多段命中，3 假人吃伤(3,3)。
   - **上挑**(↑K)：小跳+斩击+冲击波，命中。
   - **突进斩**(→→K)：突进+青色轨迹+残影，连段多命中(3,3,3)。见 `qa4_dash_early.png`。
   - **大招**(V,满怒气)：弹出"铁剑·终结技"名号 + 屏闪 + 满屏弹幕，**一发清掉 3 假人**(击杀+升级)，怒气槽清空。打击感强。
5. **资源逻辑正确**（代码）：`_cast` 校验 CD/技力（不足弹"技力不足"）、扣蓝、攒怒气、按武器分派；大招校验满怒气、释放清空。存档由 9.x 之外系统覆盖。

## ⚠️ 发现项

### 1. 〔中优先〕突进斩/群体击杀触发运行时 ERROR：`monitoring` 在碰撞信号期间被阻塞
- **现象**：`SHOT_SKILL=dash` 连拍时控制台报 `ERROR: Function blocked during in/out signal. Use set_deferred("monitoring", true/false).`（出现 2 次）。
- **根因**：突进斩(640速)瞬间冲过多个敌人，触发敌人死亡处理 `enemy.gd:316-317`（`collision_layer = 0` / `touch.monitoring = false`）。该处在物理碰撞 in/out 信号派发窗口内被同步调用 → 被 Godot 阻塞 → **赋值没生效**。
- **后果**：被阻塞意味着死亡敌人的 `touch` 接触碰撞可能没能及时关闭 → 冲过去秒杀的瞬间玩家**可能仍吃一下接触伤害**；并刷错误日志。同类模式还需排查 `player.gd:175`、`boss.gd:260`。
- **建议**：把这些 `monitoring` / `collision_layer` 赋值改为 `set_deferred("monitoring", false)` / `set_deferred("collision_layer", 0)`。
- **复现确认**：请 dev 在真实战斗里冲过 ≥2 个相邻敌、或大招清一群敌，看控制台是否报同样错、死亡敌是否还能擦伤玩家。
  > 注：QA 连拍把 3 假人摆得较密（offset 70/150/230），比常规刷怪密；但"突进/大招打多个敌"是该技能的核心用途，场景真实，建议确认而非忽略。

### 2. 〔记录/非阻塞〕开发线程自查已列的已知边界（接受）
- K 仅在 NORMAL 态响应、无输入缓冲（连段尾接技的缓冲留 9.4 手感打磨）。
- 重锤/蒸汽炮的上挑/突进/大招暂用通用实现 → 9.3 补专属变体。
- 完整招式表 UI 未做（HUD 已有文字提示）→ 可后续加。

## 诚实边界
- 技能特效用"连拍直接 arm 搓招窗口/给满怒气"拍出（专为看效果）；**键盘真实搓招的容错率**未由工具复现，靠代码审查 + 人工试玩。
- 上挑"挑飞接空连"、突进无敌帧等**手感**建议人工试玩一次确认（静态连拍只证明效果触发与命中，不证明手感节奏）。

---

## 结论
**9.2 功能验收通过**：技能系统工作、4 技能特效到位、HUD 技力/怒气达标、K过载与搓招护栏正确。
**但有 1 个中优先运行时错误（突进/群杀时 `monitoring` 被阻塞）建议修复**后再上 9.3（9.3 会用更多群体技能，问题会更频繁）。低优先项与已知边界接受延后。

🚽 QA 厕所验讫。
