# 12.4 QA：开场、结局演出与教学引导

日期：2026-07-16

## 交付范围

- 四幕序章：熄灭之城、天幕之外、零号班次、残响苏醒；支持 Enter、空格、鼠标推进和 Esc/按钮跳过。
- 六步情境教学：移动、跳跃、攻击、冲刺、交互、地图；直接观察真实输入，不暂停游戏，不吞掉地图/NPC 的按键事件。
- 三幕终章：伪王停机、七区重连、黎明之前；末页提供“继续探索”和“返回标题”。
- 独立 `cinematic_data.gd`、`cinematic_panel.gd`、`tutorial_guide.gd`，主场景只负责接线、输入锁和持久化。
- `intro_seen`、`tutorial_complete`、`ending_seen` 写入现有 `story_flags`；已有多房间进度的旧存档不强制重新教学。
- 标题页可见构建标记更新为“阶段 12.4 · 叙事演出与教学”。

## TDD 证据

`tests/test_stage_12_4.gd` 首次运行退出码 1，明确报告三份资源、分镜数据、主场景接线、状态标记、QA 钩子和标题标记均缺失。基础实现后转为 GREEN。

视觉复审发现教学卡覆盖主角出生位置，先将卡片移至左上 HUD 下方再复拍。代码复审发现最后一步 `M` 可能在地图先更新 `menu_open` 后被轮询逻辑错过；先新增要求原始输入观察器的失败断言，再改为 `_input(event)` 观察但不消费事件，回归重新 GREEN。

## godot-capture 目视验收

- `screenshots/12.4/intro.png`：序章第二幕紫色主题、程序化机械城远景、电影黑边、四幕计数、跳过与推进提示完整；背景 HUD 被正确压暗。
- `screenshots/12.4/tutorial-final.png`：教学卡位于左上生命/武器 HUD 下方，不再遮挡主角、存档点、NPC、任务追踪器或底部按键栏；键位与说明清楚。
- `screenshots/12.4/ending.png`：最终幕金色标题、三幕计数、世界观正文、继续探索/返回标题按钮完整显示。
- 三次图形运行均由源码编译 Godot `4.7.rc.custom_build.df6235838` 生成，输出未出现 `ERROR` / `SCRIPT ERROR`。

## 回归与发布

- 全部 12 个契约通过：下行入口、阶段 10.5、10.6、10.6.3、10.6.4、10.6.5、10.7、10.8、12.1、12.2、12.3、12.4，`TOTAL=12 FAILED=0`。
- 编辑器全项目解析退出码 0；主场景无头运行退出码 0，无运行期 `ERROR` / `SCRIPT ERROR`。提交后复验仅出现 godot-capture 规则明确允许过滤的退出期 `ObjectDB instances` / `1 resources still in use` 缓存噪声。
- 使用源码编译 `Godot Engine v4.7.rc.custom_build.df6235838` 与项目自定义 Windows `template_release` 导出。
- `E:\Godot\release\暗影机械城\ShadowMechCity.exe`：68,143,616 bytes，2026-07-16 23:47:47，SHA-256 `1529C04A88DAAAD7C2683259388EBBE1B730F10F9DC9FE9A1EA81C0CE7619B0F`。
- `ShadowMechCity.pck`：10,798,900 bytes，2026-07-16 23:47:48，SHA-256 `455319D1FE6B7220F86EBC3332BA472A0CB67F0403E98312D85135DE6B9801D9`。
- 从发布目录直接启动 EXE 并生成 `screenshots/12.4/release-ending.png`，退出码 0、无错误；实图与编辑器最终幕一致，确认 12.4 脚本已打入 PCK。

## 诚实边界

- 自动截图验证视觉和流程入口，连续按键推进、长时间教学节奏及最终 Boss 正常击杀后的情绪衔接仍建议策划用发布 EXE 完整试玩。
- 本阶段使用程序化剪影和现有场景，不新增第三方素材；阶段 13 可在保持流程接口的前提下替换为正式逐帧演出和区域音乐。
