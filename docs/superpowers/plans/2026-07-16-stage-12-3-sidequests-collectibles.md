# 阶段 12.3：支线任务与收藏档案实施计划

## 目标

把 10.8 的七个隐藏房和记忆核心接入 12.x 叙事系统，在现有 `N` 任务日志内加入“主线 / 支线 / 收藏”三页签；所有进度从既有存档事实回算。

## 实施批次

1. 新增 12.3 契约测试，先验证缺少支线数据、收藏档案、运行时求值和 UI 页签时失败。
2. 扩展 `quest_data.gd`：五名 NPC 各一条支线；新增七区域记忆档案元数据。
3. 扩展 `quest_runtime.gd` 与 `Game.quest_snapshot()`：支持计数型目标、NPC 解锁条件、旧存档事实回算。
4. 重构 `quest_panel.gd` 为三页签，共用左侧列表与右侧详情；收藏页显示未发现轮廓、已发现标题与档案正文。
5. 增加 12.3 截图钩子，运行全回归、错误扫描和 godot-capture，目视检查后修正。
6. 写 `docs/qa/12.3-review.md`、更新 `DEV_PLAN.md`，提交 Git，并用源码编译 Godot 导出到 `E:\Godot\release\暗影机械城`。

## 延后但已确认的技能树约束

技能树后续重做为基础、移动/身法、战斗、探索独立页面与树状圆形节点；选中节点外圈高亮，右侧展示技能动画和详情。战斗页按刀剑、铁锤、蒸汽炮/手枪、双刀、长枪、弓弩等武器拆分支线。本阶段仅记录，不实现。
